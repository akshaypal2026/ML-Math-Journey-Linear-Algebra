## 🏁 Final Summary for Video Logs

| Section | Tool | What it does | Data Science Purpose |
| :--- | :--- | :--- | :--- |
| **1. The Solver** | `np.linalg.solve` | Finds the **Weights** | Discovers the "secret logic" used to predict outcomes. |
| **2. The Auditor** | `np.linalg.matrix_rank` | Finds the **Rank** | Audits the "actual amount of info" and removes copy-cat data. |
| **3. The Visualizer** | `plt.quiver` | Sees **Independence** | Visualizes the "direction of data" and its reach (Span). |
